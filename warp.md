# Warp Quickstart

- `cd frontend && npm install`
- `cd frontend && npm run dev`
- `cd infra/terraform && terraform init && terraform plan`
- `cd backend && make build`
- `export NEXT_PUBLIC_API_BASE_URL=...` for FE
- `cd backend && make test` to run unit tests
- `cd backend && make format` before committing Python changes
